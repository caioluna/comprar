import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
  },
  itemText: {
    flexDirection: "row",
    flex: 1,
    color: "#1E1E1E"

  },
})